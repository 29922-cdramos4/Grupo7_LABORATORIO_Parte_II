# Grupo7_LABORATORIO_Parte_II

This repository contains the files for "Grupo 7 - Laboratorio Parte II".

Contents:
- `index.html` — main page
- `css/` — stylesheets (contains `estilos.css`)
- `img/` — images used by the site (includes `img1.avif`)

How to open
1. Open `index.html` in your browser (double-click or use a local server).

Notes
- This is a static web project (HTML/CSS). No build step required.
